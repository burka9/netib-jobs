import { ChatMemberUpdated } from "node-telegram-bot-api";
import { _user } from "../../interface/api";

export default async (user: _user, my_chat_member: ChatMemberUpdated, update_id: number) => {
	// console.log(my_chat_member)
}
